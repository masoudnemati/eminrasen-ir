import React from "react";
import SongList from "../Components/SongList";

const Songs = () => {
  window.scrollTo(0, 0);
  return (
    <div>
      <SongList />
    </div>
  );
};

export default Songs;
